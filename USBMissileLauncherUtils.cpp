

/*

int main() {

	int Dx = 0;	// de 0 à 640
	int Dy = 0; // de 0 à 480

///////////



  int device_type = 1;

  init(&device_type);

  traitement(Dx,Dy);

  //action(&device_type,commande);

  missile_usb_destroy(control);

  return 0;
}
*/
